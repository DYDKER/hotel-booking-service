[project]
name = "hotel-booking-service"
version = "0.1.0"
description = ""
authors = [
    {name = "sultan",email = "utegenov.sultan@inbox.ru"}
]
requires-python = ">=3.14"
dependencies = [
    "django (>=5.2,<6.0)",
    "pytest (>=9.0.0,<10.0.0)",
    "ruff (>=0.15.20,<0.16.0)",
    "mypy (>=2.2.0,<3.0.0)",
    "typing-extensions (>=4.16.0,<5.0.0)",
    "bandit (>=1.9.4,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
